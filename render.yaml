services:
  - type: web
    name: sabi-biz-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install --production
    startCommand: node start.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
