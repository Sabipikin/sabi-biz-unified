services:
  - type: web
    name: sabi-biz-backend
    env: node
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: node render-test.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
